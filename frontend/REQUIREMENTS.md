> Note: This is the file I gave as prompt to Antigravity to handle the leftover UI-related tasks.

I'm working on a mini visa-alert tracker app which allows us to perform CRUD operations on alerts.

The backend has already been set up. The state management and API (using RTK Query) has also been setup on frontend. All that's left is to create the UI and connect it to the API.

Your task is to handle the UI. You are required to create a beautiful single page UI with the following features:

- A form to create new alert
- A table to display all alerts (with pagination and filters)
- Functionality to update alert.
- Functionality to delete alert.
- Graceful error handling and UI feedback.
- Aesthetic, responsive and mobile-friendly design.
- Dark mode support.

Basically, we need a single page with a form to add new alerts and a table as described above. No need for multiple pages.

You will be provided with relevant context about the schemas and the API. Use that to create the UI.
